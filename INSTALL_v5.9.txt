BETYNZ v5.9 — PERMANENT GAME POPULATION FIX

RECOMMENDED: use the v5.9 patch over your existing repository. It does not contain data.js, so your current live board is not overwritten.

1. Extract the patch into the root of the betynz-web repository.
2. Allow the listed files to replace the old versions.
3. Commit and push with:
   Prevent empty deployments and preserve verified game data
4. Open GitHub Actions.
5. Run Smart Global Coverage and Deep Enrichment once.
6. After it succeeds, run Refresh Live & Finished Scores and Deploy once.

The UI deployment workflow now restores the newest non-empty data.js from Git history before deploying. If no verified board exists, it fails safely instead of publishing an empty website.
