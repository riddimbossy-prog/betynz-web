BETYNZ v6.1 INSTALL

1. Extract the patch into the betynz-web repository root.
2. Replace matching files.
3. Do not delete or replace data.js.
4. Commit: Restore lightweight Zeus experience and speed up startup
5. Push to GitHub.
6. Run: Actions > Deploy Betynz Product > Run workflow.
7. Close all open Betynz tabs and PWA windows, then reopen once.

The first visit shows Zeus and the guided tour. The Zeus launch appears once per browser session. The tour appears once per device and can be replayed from Methodology.
