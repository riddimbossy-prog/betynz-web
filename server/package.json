{
  "name": "betspapa-api",
  "version": "1.8.3",
  "private": true,
  "description": "BetsPapa Render API with Supabase, API-Football importers, HT/FT profile builder, common-sense prediction engine, and grading.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
