{
  "name": "betynz-api",
  "version": "6.2.0",
  "private": true,
  "description": "Betynz live database API with Supabase fixture sync, prediction generation, automatic grading, and public dashboard endpoints.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
